name: jashn_e_fabric
description: "Jashn-e-Fabric brings you premium ethnic wear crafted with elegance and care. From graceful kurtis to festive fabrics, each design celebrates tradition with a modern touch. Perfect for everyday wear and special occasions. ✨"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.2.1
  google_fonts: ^6.1.0
  responsive_framework: ^1.1.1
  animations: ^2.0.8  # For pre-built animations
  spring: ^2.0.2  # For spring-based animations

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/

